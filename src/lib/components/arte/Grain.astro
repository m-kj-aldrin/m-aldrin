---

---

<svg width="100%" height="100%">
  <filter id="filter-grain">
    <feTurbulence
      baseFrequency="0.4"
      numOctaves="1"
      seed="1"
      type="fractalNoise"
      result="smal"></feTurbulence>
    <feTurbulence
      baseFrequency="0.003"
      numOctaves="1"
      seed="1"
      type="fractalNoise"></feTurbulence>
    <feComposite in="smal" operator="arithmetic" k1="0.3" k2="0.1" k3="-0.25"
    ></feComposite>
    <feColorMatrix type="saturate" values="0"></feColorMatrix>
  </filter>
  <rect
    x="0"
    y="0"
    width="100%"
    height="100%"
    filter="url(#filter-grain)"
    opacity="1"></rect>
</svg>

<style>
  svg {
    display: block;
    position: fixed;
    inset: 0;
    contain: strict;
    pointer-events: none;
    z-index: 100;
  }
</style>
